// To be decided: should types be in their own named files and collated here,
// or just lobbed in one file? as long as index.tsx exports them all I don't mind
type SiteConfig = {
  description: string;
};

export type { SiteConfig };
